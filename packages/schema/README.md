# @coldtea/abide-schema

The contract for [Abide](https://github.com/coldteadotai/abide): zod schemas for the rubric a coding agent compiles from a repository's instruction files, the hook payloads each host sends the checker, and the verdicts and events the checker writes. Everything else in Abide consumes this package.
