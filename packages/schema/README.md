# @coldtea/abide-schema

The contract for [Abide](https://github.com/coldteadotai/abide): zod schemas for the rubric a coding agent compiles from a repository's instruction files, the Claude Code hook payloads the checker reads, and the verdicts and events it writes. Everything else in Abide consumes this package.
